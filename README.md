## 📤 Команды для полного пуша на GitHub

```bash
git init
git add .
git commit -m "Модуль 2 и 3: ИгроМир - система управления партнерами и продукцией"
git branch -M main
git remote add origin https://github.com/Anna777-111-000/KR1.git
git push -u origin main
```

Если репозиторий уже подключён:
```bash
git add .
git commit -m "Модуль 2 и 3: ИгроМир - система управления партнерами и продукцией"
git push
```

---

## 📁 Список файлов проекта

### ✅ Созданные/изменённые файлы:

| Файл | Что сделано |
|---|---|
| **App.xaml** | Настроены глобальные стили: цвета #156082, #7cdf9a, шрифт Segoe UI |
| **MainWindow.xaml** | Главная форма со списком карточек продукции, шапкой, кнопками |
| **MainWindow.xaml.cs** | Загрузка продукции из БД, алгоритм расчёта скидки, обработчики кнопок, класс `ProductCard` |
| **ProductEditWindow.xaml** | Форма добавления/редактирования продукции (наименование, тип, цена, количество) |
| **ProductEditWindow.xaml.cs** | Логика добавления/редактирования, валидация полей, сохранение в БД |

### 📦 Исходные файлы (из задания):

| Файл | Назначение |
|---|---|
| **KR_3.sql** | Скрипт создания и заполнения БД (таблицы Products, ProductTypes, Partners, PartnerTypes, Managers, Employees, Posts, Applications и др.) |
| **/Images/ico.jpg** | Логотип компании для отображения в приложении |
| **logo.ico** | Иконка приложения |

